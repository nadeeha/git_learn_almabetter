# Cell 1 — install if needed
!pip install plotly pandas numpy

# Cell 2 — import and run
import sys
sys.path.append('/path/to/folder/containing/script')  # if not in same folder
from stability_dashboard import build_dashboard
import pandas as pd

# Load your rerun results
df = pd.read_csv('rerun_results.csv')

# Build the dashboard
stats = build_dashboard(
    df,
    output_path='stability_dashboard.html',
    n_runs=5,
    title='Defense theme — entity prompt stability (Short variant, Gemini 2.5 Pro)'
)

# Optionally inspect the per-entity stats inline
stats.head()

from IPython.display import IFrame
IFrame('stability_dashboard.html', width='100%', height=800)
